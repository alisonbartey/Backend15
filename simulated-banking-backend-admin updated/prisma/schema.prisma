# 1. Save the schema above to prisma/schema.prisma

# 2. Create migration
npx prisma migrate dev --name full_schema_setup

# 3. Generate client
npx prisma generate

# 4. (Optional) Seed with test data
npx prisma db seed
